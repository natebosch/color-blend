name: colorblend
description: Find the resulting colors with an alpha applied on a background

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  angular: ^5.2.0

dev_dependencies:
  build_runner: ^1.2.0
  build_test: ^0.10.6
  build_web_compilers: ^1.1.0
  test: ^1.3.0
